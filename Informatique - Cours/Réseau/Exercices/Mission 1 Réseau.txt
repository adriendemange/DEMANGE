Mission 1 Réseau : 

Etape 1 : Le masque de sous-réseau est 255.255.0.0 (CIDR=16)
Le sous réseau de l'adresse IP est 198.168 (car 16 bits sont consacrés au réseau)
Il faut un équipement (type switch) pour que les machines puissent communiquer entre elles.
Non car il n'y a pas d'équipement pour assurer la communication
La commande "ipconfig" sur windows 
La commande ping permet d'envoyer des données à une machine grâce à l'adresse IP

Etape 2 :
Le switch a été ajouté et permet la communication de données entre les machines du même réseau.
Un hub permet de concentrer des machines sur un même point. Cela pourrait assurer une communication entre elles.
Non car il faut changer l'adresse IP pour qu'elles soient toutes unique.

On peut faire cela avec l'adresse de diffusion

Etape 3 :
La machine 4 appartient au sous réseau "192.169" qui est différent du sous réseau "192.168".
La différence avec le sous réseau dans l'adresse IP explique le fait qu'elle ne peut pas communiquer avec les autres.
Il faut mettre la même adresse réseau sur la machine 2 pour communiquer avec M4
Non car il n'y a pas la même partie de sous réseau dans l'adresse IP 